from selenium import webdriver
from selenium.webdriver.chrome.options import Options
from selenium.webdriver.chrome.service import Service

# Créer un objet options
options = Options()
# Ajouter des options selon vos besoins
options.add_argument("--headless")
options.add_argument("--disable-gpu")

# Créer un objet service avec le chemin du driver
service = Service("C:\\Users\\user\\Downloads\\chromedriver.exe")

# Créer un objet WebDriver pour Chrome avec le service et les options
driver = webdriver.Chrome(service=service, options=options)
